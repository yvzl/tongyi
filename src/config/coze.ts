const cozeUrl = import.meta.env.VITE_COZE_URL;
const cozeToken = import.meta.env.VITE_COZE_TOKEN;
const cozeBotId = import.meta.env.VITE_COZE_BOT_ID;
const cozeUserId = import.meta.env.VITE_COZE_USER_ID;

export {
    cozeUrl,
    cozeToken,
    cozeBotId,
    cozeUserId
}